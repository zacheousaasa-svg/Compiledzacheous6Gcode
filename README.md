# Generative Foundation Models for Autonomous Resource Optimization in RIS-Assisted SAGIN Edge Intelligence

This repository provides the complete execution environment, dataset tracking vectors, physical channel modeling parameters, and baseline replication scripts for the proposed framework.

---

## Quick Start & Replication Guide

To verify the experimental simulation framework and completely regenerate all manuscript artifacts (including Tables 1a–1d, Tables 2a–2b, the Master Performance Evaluation Matrix, and Figures 2–5) at high resolution, clone this repository to your local machine or cloud environment and execute the following sequential commands:

```bash
# 1. Run the main compilation pipeline to build data logs and parameter structures
python3 compile_reproducibility_package.py

# 2. Install the isolated, pinned dependency libraries auto-generated by the script
pip install -r requirements.txt

# 3. Verify that all data matrices and 1200 DPI vector plots exist locally
ls -la Table_*.csv Figure*.png Fig*.png sagin_simulated_dataset.csv
---

## Re-Generated Publication-Grade Figures (1200 DPI)

* **`Fig2_GFM_Distillation.png`**: Illustrates the cloud-to-edge lightweight knowledge distillation framework mapping Kullback-Leibler ($\mathcal{L}_{KL}$) soft target distributions.
* **`Fig3a_PIDT_Synchronization.png`**: Traces the physics-informed digital twin closed-loop synchronization engine enforcing electromagnetic wavefront boundary limits.
* **`Fig3b_Methodology_Flowchart.png`**: Displays the unified 5-phase control-plane operational execution sequence.
* **`Figure_4_Adaptability_Analysis.png`**: Validates online model tracking score re-convergence following the non-stationary abrupt network topology shock at episode 500.
* **`Figure5_Pareto_FINAL_1200dpi.png`**: Traces the multi-objective scattered candidate sweep outlining the non-convex Rate-Energy ($R$-$E$) Pareto frontier.

---

## Exported Technical Tables (Structured CSV)

* **`Table_1a_Comparative_Analysis.csv`**: Structural literature mapping matrix delineating the algorithmic novelties of our GFM edge architecture against sequence and DRL frameworks.
* **`Table_1b_Mathematical_Notations.csv`**: Comprehensive notation index mapping explicit variable tensor space dimensions ($\zeta_{sem}$, $E_{DRL}$, $\epsilon_{distill}$).
* **`Table_1c_Control_Plane_Paradigms.csv`**: Functional matrix comparing traditional task-oriented RL and language-based orchestrators.
* **`Table_1d_Baseline_Configurations.csv`**: Structural profile detailing learning rates, layer configurations, and fixed cross-framework random seeds ($Index = 42$).
* **`Table_2a_Simulation_Environment.csv`**: Pinned operational specifications for the multi-layer space-air-ground integrated edge network environment.
* **`Table_2b_Environment_Constants.csv`**: Physical channel constants detailing path loss exponents and cascading Rician fading assumptions ($K = 10 \text{ dB}$).
* **`Table_Master_Results_Matrix.csv`**: The complete benchmark evaluation log displaying performance metrics with standard deviations evaluated across 50 independent simulation trials.

---

## Environmental Dependencies & Technical Specifications

The auto-generated `requirements.txt` file locks down exact, mutually compatible software environment versions to guarantee absolute replication cross-platform:
* **Python Engine**: `python >= 3.10`
* **Numerical Core**: `numpy==1.26.4`, `pandas==2.2.2`
* **Visualization Layer**: `matplotlib==3.8.4`
* **Evaluation Metrics**: `scikit-learn==1.4.2`

All graphics are compiled using a headless backend (`matplotlib.use('Agg')`), ensuring that vector assets render flawlessly on any remote cloud server, headless Linux terminal, or container environment without requiring active window popup displays.
